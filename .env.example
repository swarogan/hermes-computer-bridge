# Secrets only. This plugin does not need API keys for local portal capture.
# Restore tokens are not secrets in the cloud sense but they ARE session
# capability grants — they live in XDG state, never in git.
